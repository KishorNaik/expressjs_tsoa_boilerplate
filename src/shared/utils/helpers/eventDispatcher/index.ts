import { EventDispatcher, EventDispatcherRunner } from '@kishornaik/utils';

const eventDispatcher = new EventDispatcher();
const eventDispatcherRunner = new EventDispatcherRunner();

export { eventDispatcher, eventDispatcherRunner };
